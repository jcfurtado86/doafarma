import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  group: {
    width: '100%',
    height: 56,
    backgroundColor: '#F7F7F7',
    borderRadius: 10,
    flexDirection: 'row',
    alignItems: 'center',
    overflow: 'hidden',
  },
  control: {
    flex: 1,
    height: 56,
    width: '100%',
    borderRadius: 10,
    paddingLeft: 16,
    fontSize: 16,
  },
});
